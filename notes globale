🔹1. minishell.h (toi)

🧠 But : déclarer toutes les struct, prototypes, macros, includes, variable globale g_exit_code
🔹2. lexer.c (toi)

✂️ But : couper la ligne d’entrée en morceaux (tokens bruts)
Exemples :

    "ls -l | grep a" → ["ls", "-l", "|", "grep", "a"]

    gérer partiellement les quotes (identification simple)

🔹3. quote_utils.c (toi ou belle gosse)

📐 But : détecter les quotes ouvertes/fermées, fusionner ce qui est entre quotes, etc.
Fonctions utiles :

    is_closed_quote(char *str)

    skip_quote(char *str, int i)

🔹4. token_utils.c (toi)

🏷 But : transformer les morceaux en t_token avec un type : mot, pipe, redirection, etc.

    Créer la liste chaînée des tokens

🔹5. syntax_checker.c (toi ou belle gosse)

🧯 But : détecter les erreurs : |, >, < mal utilisés, quotes non fermées
🔹6. env_utils.c (toi)

🌍 But : outils pour manipuler l’environnement (envp), chercher des variables

    get_env_value(char *name)

    copy_env(char **envp)

🔹7. expander.c (toi ou belle gosse)

💸 But : remplacer $USER, $?, etc. par leurs valeurs réelles dans les tokens

    Respecter les règles de quotes

🔹8. cmd_builder.c (toi)

🏗 But : convertir la liste de t_token en une liste de t_cmd

    Chaque t_cmd contient : args, redirs, type, pipe, etc.

🔹9. builtin.c (belle gosse)

🧩 But : implémenter les builtins :

    echo, cd, pwd, env, export, unset, exit

🔹10. redir.c (belle gosse)

🛠 But : gérer les redirections :

    <, >, >>, << → open(), dup2()

🔹11. pipe.c (belle gosse)

🚰 But : mettre en place les pipe() et gérer les file descriptors entre commandes
🔹12. executor.c (belle gosse)

🧨 But : boucle principale d’exécution

    parcourt t_cmd, fork, execve, attend, builtin, redirs, etc.

🔹13. signal.c (toi ou belle gosse)

🔔 But : définir les handlers pour SIGINT, SIGQUIT, etc.

    comportement interactif attendu

🔹14. main.c (à deux)

🌀 But : boucle de base : readline → parsing → execution → free
